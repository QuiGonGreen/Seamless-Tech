














}    // ...existing function code...    }        return;        };            body: "Invalid or missing API key"            status: 401,        context.res = {    if (!apiKey || apiKey !== validApiKey) {    const apiKey = req.headers["x-api-key"];    const validApiKey = process.env.CUSTOM_API_KEY;module.exports = async function (context, req) {